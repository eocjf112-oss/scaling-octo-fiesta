# ============================================================
# Jarvis AI Assistant — 의존성 목록
# Python 3.10+ 필요
# ============================================================

# AI 프로바이더
openai>=1.30.0
anthropic>=0.28.0

# Open Interpreter
open-interpreter>=0.2.4

# CLI UI
rich>=13.7.0
prompt_toolkit>=3.0.43
click>=8.1.7

# 환경 설정
python-dotenv>=1.0.0

# HTTP 클라이언트
httpx>=0.27.0
requests>=2.31.0

# 데이터 처리
pydantic>=2.7.0
pyyaml>=6.0.1

# 날짜/시간
python-dateutil>=2.9.0

# 파일 처리
pathlib2>=2.3.7; python_version < "3.4"
watchdog>=4.0.0

# 로깅
loguru>=0.7.2

# 비동기
asyncio-mqtt>=0.16.0; extra == "mqtt"
aiofiles>=23.2.1

# Windows 호환성
pyreadline3>=3.4.1; sys_platform == "win32"
colorama>=0.4.6; sys_platform == "win32"

# 선택적 — 음성 (Voice)
# speechrecognition>=3.10.0
# pyttsx3>=2.90
# pyaudio>=0.2.14
